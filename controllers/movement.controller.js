
/**
 * recorrer los productos enviados encontrarlos y updatear el stock. 
 * despues guardar el movimiento
 */
const createMovement = ( req, res ) => {
    
}

/** Update movement esta en veremos */
const updateMovement = ( req, res ) => {

}

const deleteMovement = ( req, res ) => {

}

module.exports = {
    createMovement,
    updateMovement,
    deleteMovement
}